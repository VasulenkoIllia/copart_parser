{
  "name": "copart_parser",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "db:migrate": "npm run build && node dist/index.js db:migrate",
    "db:reset": "./scripts/db-reset.sh",
    "db:drop": "./scripts/db-drop.sh",
    "ingest:csv": "npm run build && node dist/index.js ingest:csv",
    "photo:sync": "npm run build && node dist/index.js photo:sync",
    "photo:cluster": "npm run build && node dist/index.js photo:cluster",
    "proxy:check": "npm run build && node dist/index.js proxy:check",
    "pipeline:run-once": "npm run build && node dist/index.js pipeline:run-once",
    "retention:cleanup": "npm run build && node dist/index.js retention:cleanup",
    "scheduler:start": "npm run build && node dist/index.js scheduler:start",
    "telegram:bot": "npm run build && node dist/index.js telegram:bot",
    "test:photo-update": "node ./scripts/photo-update-test-suite.mjs",
    "test:schema-contract": "node ./scripts/schema-contract-test.mjs",
    "test:runtime-guards": "node ./scripts/runtime-guard-test.mjs",
    "proxy:probe-copart": "node ./scripts/probe-copart-proxies.mjs",
    "proxy:probe-compare": "node ./scripts/probe-copart-proxies.mjs --compare-tls --direct-only",
    "proxy:probe-mmember": "node ./scripts/probe-copart-proxies.mjs --direct-only",
    "proxy:probe-mmember-proxies": "node ./scripts/probe-copart-proxies.mjs --proxy-only",
    "proxy:probe-residential": "node ./scripts/probe-copart-proxies.mjs --direct-only --residential-proxy"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "csv-parse": "^6.1.0",
    "dotenv": "^17.3.1",
    "http-proxy-agent": "^5.0.0",
    "https-proxy-agent": "^5.0.1",
    "image-size": "^2.0.2",
    "mysql2": "^3.19.1",
    "node-cron": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "typescript": "^5.5.3"
  },
  "private": true
}
